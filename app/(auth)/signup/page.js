import SignupForm from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Create Account — TalentGraph AI',
  description: 'Sign up for TalentGraph AI',
};

export default function SignupPage() {
  return <SignupForm />;
}
